<div class="card card-styling">
    <div class="card-body-styling">
        <div class="card-title card-title-styling">
            <h2>{{title}}</h5>
                <p>
                    Posted by {{user.name}} on {{format_date date_created}}
                </p>
        </div>
        <p class="card-text card-text-styling">{{contents}}</p>
    </div>
</div>